"1. Define the method"
Integer extend [
    fib [
        (self <= 2) ifTrue: [ ^1 ].
        ^ (self - 1) fib + (self - 2) fib
    ]
].

"2. Calculate the 10th Fibonacci number"
| result |
result := 10 fib.

"3. Print to the transcript/console"
('The 10th Fibonacci number is: ', result printString) displayNl.

