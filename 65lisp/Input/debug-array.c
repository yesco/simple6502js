word a,b,c;
char sa[]="foo";
char sb[]="foo";
char sc[]={'f','o','o',0};
word main(){
  puth(&a); puth(&b); puth(&c); putchar('\\n');
  puth(&sa); puts(sa);
  puth(&sb); puts(sb);
  puth(&sc); puts(sc);
  return 4711;
}
